import Dashboard from './Dashboard';
import NewEmployee from './NewEmployee';
// import LoginPage from './LoginPage';

// eslint-disable-next-line import/no-anonymous-default-export
export default {
    Dashboard,
    NewEmployee
    // LoginPage
};

// I went with a best practice approach i learnt from a previous project
// to organise all the imports in one file so you have to import only this 
// file when needed.